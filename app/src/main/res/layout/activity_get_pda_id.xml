<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".GetPdaId"
    android:background="@drawable/app_back" >

    <TextView
        android:id="@+id/pdaview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_margin="10dp"
        android:layout_marginLeft="25dp"
        android:text="PDA ID"
        android:textColor="#000000"
        android:textSize="20sp"
        android:padding="5dp" />

    <Button
        android:id="@+id/submit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/pdaview"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/pdaview"
        android:background="@drawable/web_btn_default_pressed_holo_light"
        android:gravity="center_horizontal"
        android:text="Submit"
        android:textColor="#ffffff"
        android:textSize="25sp" />

    <EditText
        android:id="@+id/pdaid"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignRight="@+id/submit"
        android:layout_margin="10dp"
        android:layout_toRightOf="@+id/pdaview"
      
        android:ems="10"
        android:inputType="number"
        android:hint="Enter your PDA ID"
        android:padding="5dp" >

        <requestFocus />
    </EditText>

</RelativeLayout>
